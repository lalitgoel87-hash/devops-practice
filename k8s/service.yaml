apiVersion: v1
kind: Service
metadata:
  name: devops-practice-service
spec:
  selector:
    app: devops-practice
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080